# circle.yml
version: 2.1
jobs:
  start-server:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run:
          command: |
            npm install
          name: Install
      - run:
          command: |
            npm run start
          name: Start Server

workflows:
  main:
    jobs:
      - start-server
      - node/test
