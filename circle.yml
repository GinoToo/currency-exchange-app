# circle.yml
orbs: # declare what orbs we are going to use
  node: circleci/node@2.0.2
version: 2.1
jobs:
  start-server:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run:
          command: |
            npm install
          name: Install
      - run:
          command: |
            npm run start
          name: Start Server

workflows:
  main:
    jobs:
      - start-server
      - node/test
